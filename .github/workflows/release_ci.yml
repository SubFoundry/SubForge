name: release-ci

on:
  push:
    branches:
      - main
      - develop
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  RELEASE_ARTIFACT_RETENTION_DAYS: 14
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  quality-gate:
    name: quality-${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-22.04
          - windows-latest
          - macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Linux desktop build dependencies
        if: runner.os == 'Linux'
        run: |
          export DEBIAN_FRONTEND=noninteractive
          for attempt in 1 2 3; do
            sudo apt-get update && sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev patchelf && break
            if [ "$attempt" -eq 3 ]; then
              echo "apt install failed after ${attempt} attempts"
              exit 1
            fi
            sleep $((attempt * 15))
          done
      - uses: dtolnay/rust-toolchain@1.94.1
        with:
          components: rustfmt
      - name: Ensure rustfmt component is available
        run: rustup component add rustfmt --toolchain 1.94.1
      - name: Cargo fmt check
        run: cargo fmt --all -- --check
      - name: Cargo check
        run: cargo check --workspace
      - name: Cargo test
        run: cargo test --workspace

  static-security-gate:
    name: static-security-gate
    runs-on: ubuntu-22.04
    needs:
      - quality-gate
    steps:
      - uses: actions/checkout@v4
      - name: Install Linux desktop build dependencies
        run: |
          export DEBIAN_FRONTEND=noninteractive
          for attempt in 1 2 3; do
            sudo apt-get update && sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev patchelf && break
            if [ "$attempt" -eq 3 ]; then
              echo "apt install failed after ${attempt} attempts"
              exit 1
            fi
            sleep $((attempt * 15))
          done
      - uses: dtolnay/rust-toolchain@1.94.1
        with:
          components: clippy
      - name: Ensure clippy component is available
        run: rustup component add clippy --toolchain 1.94.1
      - uses: taiki-e/install-action@cargo-audit
      - uses: pnpm/action-setup@v4
        with:
          version: 10.27.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile
      - name: Cargo clippy (deny warnings)
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: Cargo audit
        run: |
          for attempt in 1 2 3; do
            cargo audit && break
            if [ "$attempt" -eq 3 ]; then
              echo "cargo audit failed after ${attempt} attempts"
              exit 1
            fi
            sleep $((attempt * 20))
          done
      - name: Pnpm audit (high+)
        run: pnpm audit --audit-level=high --registry https://registry.npmjs.org

  core-release:
    name: core-release-${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs:
      - quality-gate
      - static-security-gate
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-22.04
          - windows-latest
          - macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@1.94.1
      - name: Build subforge-core release
        run: cargo build -p subforge-core --release
      - name: Upload core binary (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: subforge-core-${{ matrix.os }}-x86_64-pc-windows-msvc
          path: target/release/subforge-core.exe
          retention-days: ${{ env.RELEASE_ARTIFACT_RETENTION_DAYS }}
      - name: Upload core binary (Unix)
        if: runner.os != 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: subforge-core-${{ matrix.os }}-${{ runner.arch }}
          path: target/release/subforge-core
          retention-days: ${{ env.RELEASE_ARTIFACT_RETENTION_DAYS }}

  core-release-linux-musl:
    name: core-release-linux-musl
    runs-on: ubuntu-22.04
    needs:
      - quality-gate
      - static-security-gate
    steps:
      - uses: actions/checkout@v4
      - name: Install Linux musl toolchain
        run: |
          export DEBIAN_FRONTEND=noninteractive
          for attempt in 1 2 3; do
            sudo apt-get update && sudo apt-get install -y musl-tools && break
            if [ "$attempt" -eq 3 ]; then
              echo "apt install failed after ${attempt} attempts"
              exit 1
            fi
            sleep $((attempt * 15))
          done
      - uses: dtolnay/rust-toolchain@1.94.1
        with:
          targets: x86_64-unknown-linux-musl
      - name: Build subforge-core release (linux musl)
        run: cargo build -p subforge-core --release --target x86_64-unknown-linux-musl
      - name: Upload core binary (Linux musl)
        uses: actions/upload-artifact@v4
        with:
          name: subforge-core-ubuntu-22.04-x86_64-unknown-linux-musl
          path: target/x86_64-unknown-linux-musl/release/subforge-core
          retention-days: ${{ env.RELEASE_ARTIFACT_RETENTION_DAYS }}

  desktop-pipeline-smoke:
    name: desktop-pipeline-smoke-${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs:
      - quality-gate
      - static-security-gate
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-22.04
          - windows-latest
          - macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Linux desktop build dependencies
        if: runner.os == 'Linux'
        run: |
          export DEBIAN_FRONTEND=noninteractive
          for attempt in 1 2 3; do
            sudo apt-get update && sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev patchelf && break
            if [ "$attempt" -eq 3 ]; then
              echo "apt install failed after ${attempt} attempts"
              exit 1
            fi
            sleep $((attempt * 15))
          done
      - uses: pnpm/action-setup@v4
        with:
          version: 10.27.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - uses: dtolnay/rust-toolchain@1.94.1
      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile
      - name: Desktop Tauri build smoke (no bundle)
        run: pnpm -C apps/desktop tauri build --ci --no-bundle
      - name: Upload desktop binary (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: subforge-desktop-${{ matrix.os }}-x86_64-pc-windows-msvc
          path: target/release/subforge-desktop-tauri.exe
          retention-days: ${{ env.RELEASE_ARTIFACT_RETENTION_DAYS }}
      - name: Upload desktop binary (Unix)
        if: runner.os != 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: subforge-desktop-${{ matrix.os }}-${{ runner.arch }}
          path: target/release/subforge-desktop-tauri
          retention-days: ${{ env.RELEASE_ARTIFACT_RETENTION_DAYS }}

  desktop-bundle:
    name: desktop-bundle-${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    needs:
      - quality-gate
      - static-security-gate
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-22.04
          - windows-latest
          - macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Linux desktop build dependencies
        if: runner.os == 'Linux'
        run: |
          export DEBIAN_FRONTEND=noninteractive
          for attempt in 1 2 3; do
            sudo apt-get update && sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev patchelf && break
            if [ "$attempt" -eq 3 ]; then
              echo "apt install failed after ${attempt} attempts"
              exit 1
            fi
            sleep $((attempt * 15))
          done
      - uses: pnpm/action-setup@v4
        with:
          version: 10.27.0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm
      - uses: dtolnay/rust-toolchain@1.94.1
      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile
      - name: Build desktop bundle (Windows NSIS)
        if: runner.os == 'Windows'
        run: pnpm -C apps/desktop tauri build --ci --bundles nsis
      - name: Build desktop bundle (macOS DMG)
        if: runner.os == 'macOS'
        run: pnpm -C apps/desktop tauri build --ci --bundles dmg
      - name: Build desktop bundle (Linux DEB/AppImage)
        if: runner.os == 'Linux'
        run: pnpm -C apps/desktop tauri build --ci --bundles deb,appimage
      - name: Upload NSIS bundle
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: subforge-desktop-bundle-${{ matrix.os }}-x86_64-pc-windows-msvc-nsis
          path: target/release/bundle/nsis/*.exe
          if-no-files-found: error
          retention-days: ${{ env.RELEASE_ARTIFACT_RETENTION_DAYS }}
      - name: Upload DMG bundle
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: subforge-desktop-bundle-${{ matrix.os }}-${{ runner.arch }}-dmg
          path: target/release/bundle/dmg/*.dmg
          if-no-files-found: error
          retention-days: ${{ env.RELEASE_ARTIFACT_RETENTION_DAYS }}
      - name: Upload DEB bundle
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: subforge-desktop-bundle-${{ matrix.os }}-${{ runner.arch }}-deb
          path: target/release/bundle/deb/*.deb
          if-no-files-found: error
          retention-days: ${{ env.RELEASE_ARTIFACT_RETENTION_DAYS }}
      - name: Upload AppImage bundle
        if: runner.os == 'Linux'
        uses: actions/upload-artifact@v4
        with:
          name: subforge-desktop-bundle-${{ matrix.os }}-${{ runner.arch }}-appimage
          path: target/release/bundle/appimage/*.AppImage
          if-no-files-found: error
          retention-days: ${{ env.RELEASE_ARTIFACT_RETENTION_DAYS }}
